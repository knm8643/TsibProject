<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.trys.tsibproject.mapper.boardMapper">
        <insert id="savePostMapper">
            insert into tsib.board (board_title, board_media, board_tag, board_content, mem_no) values (#{title}, #{media}, #{tag}, #{content}, 1);
        </insert>
</mapper>
